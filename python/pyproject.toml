[project]
python = "^3.8"

[build-system]
requires = [
    "cmake",
    "ninja",
    "pybind11",
    "scikit-build",
    "numpy",
    "setuptools",
    "wheel",
]
build-backend = "setuptools.build_meta"

[build-system]
requires = [
    "cmake>=3.19.0",
    "pip>=21.3.1",
    "pybind11>=2.9.0",
    "wheel>=0.37.1",
    "scikit-build>=0.12.0",
    "ninja>=1.10.2; platform_system!='Windows'",
    "numpy>=1.21.4",
]

build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
